node_modules/
dist/
*.sqlite
.env

# whatsapp-web.js login session + browser cache (created by `npm run wa:login`)
.wwebjs_auth/
.wwebjs_cache/

.superpowers/

# inbound WhatsApp media (forwarded payment QRs etc.) — user data, never commit
assets/inbound/

web/node_modules/
web/dist/
